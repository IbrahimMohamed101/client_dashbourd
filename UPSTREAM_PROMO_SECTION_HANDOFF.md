# Promo section visibility fix

This branch keeps the promo-code section visible immediately before the subscription total summary on the create-subscription page. It also shows the backend-authoritative price before discount, discount value/code, and price after discount when a promo code is applied.

Target deployment repository: `Basic-Diet/client_dashbourd`.
